streamlit>=1.48
pandas>=2.0
PyYAML>=6.0
requests>=2.31            # google-auth's HTTP transport (iap token verification)
google-auth>=2.30         # iap auth provider
PyJWT[crypto]>=2.8        # iap auth provider — google-auth needs this to parse JWK-format certs

# storage.provider = gcs_parquet (default) — change-log audit trail is a
# BigQuery table, so bigquery deps are required alongside the GCS ones.
google-cloud-bigquery>=3.20
db-dtypes>=1.2
google-cloud-storage>=2.16
pyarrow>=15.0

# Optional — only needed when storage.provider = bigquery instead
# (already covered above when using the gcs_parquet default)
